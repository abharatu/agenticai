{
    // Add bin directory to PATH for all platforms
    "terminal.integrated.env.osx": {
        "PATH": "${workspaceFolder}/bin:${env:PATH}"
    },
    "terminal.integrated.env.linux": {
        "PATH": "${workspaceFolder}/bin:${env:PATH}"
    },
    "terminal.integrated.env.windows": {
        "PATH": "${workspaceFolder}\\bin;%PATH%"
    }
}