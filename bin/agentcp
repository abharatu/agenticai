#!/bin/bash
# agentcp: Run the langchain-based agent workflow (agentcp.js)
# Usage: ./bin/agentcp [args]

# Set environment variables (edit as needed)
export MODEL_PROVIDER=azureopenai

# Change to backend directory
cd "$(dirname "$0")/../ai"

# Run the langchain agent workflow
node agentcp.js "$@"